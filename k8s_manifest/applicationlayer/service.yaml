apiVersion: v1
kind: Service
metadata:
  name: application
  labels:
    app: personnel
    component: userinterface
spec:
  ports:
    - port: 8080
  selector:
    app: personnel
    component: userinterface