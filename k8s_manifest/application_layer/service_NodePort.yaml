apiVersion: v1
kind: Service
metadata:
  name: application-service
  labels:
    app: employees
    component: userinterface
spec:
  type: NodePort
  selector:
   app: employees
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30000
 